import type { Response } from "express";
import { AuthReq } from "../types/para";
import { SuperAdmin } from "../model";
export const dashboard = (req: AuthReq<SuperAdmin>, res: Response) => {
  try {
  } catch (e) {}
};
